---
# Default variables for OPNsense NAT role
# OPNsense connection details
opnsense_nat_host: "10.128.0.1"
opnsense_nat_validate_certs: false
# Authentication - these should be set in your playbook or vault
opnsense_nat_username: ""  # Required: OPNsense admin username
opnsense_nat_password: ""  # Required: OPNsense admin password
# These will be set automatically by auth.yml if not provided
opnsense_nat_phpsessid: ""  # Optional: PHP session ID from OPNsense login
opnsense_nat_csrf_token: ""  # Optional: CSRF token from OPNsense
# Example NAT rules (should be overridden in your playbook)
opnsense_nat_rules: []
